# 个人网站改进 Agent 提示词补充：立体感与异形平面设计

目标文件：`E:\下载\个人网站\index.html`

配套使用：
- `E:\参考\个人网站改进Agent提示词_明日方舟空间UI与虹彩色调.md`
- `E:\参考\明日方舟风格个人网站Agent提示词_补充_空间化UI与主控台.md`

本补充文档只解决一个问题：当前色调已经满意，但平面设计仍然太规整、太平、太像普通信息卡片，缺少《明日方舟》式令人耳目一新的空间冲击、异形图形、前后层次和 3D 感。

## 当前问题判断

从截图看，目前页面已经有不错的云层虹彩色调、冷蓝紫文字、HUD 风格导航和信息编号。但平面设计上仍存在这些问题：

- 首屏仍是“头像 + 姓名 + 四个矩形入口”的常规结构。
- 主要 UI 元素几乎都是正矩形，边缘太平直，缺少斜切、弧形、透视、错位和异形块。
- 卡片都在同一平面上，缺少前景、中景、背景之间的遮挡关系。
- 头像、标题、入口卡片没有形成一个“场景”，更像信息排版。
- 文章区和触星区仍然是大白面板分割，像表格或后台面板，视觉惊喜不足。
- 《触星》区虽然色调正确，但没有变成“项目沙盘 / 主线任务 / 3D 入口”。
- 背景网格存在，但太均匀、太安静，没有明显空间透视。
- 图形元素太少，缺少大号图形、半透明叠层、曲面屏幕、倾斜面板、漂浮物、发光轴线、几何阵列。

改进目标不是增加更多装饰，而是改变构图方式：让页面从“二维整齐信息板”变成“可操作的空间化视觉系统”。

## 总体改进目标

请继续保留当前满意的色调，不要把颜色推翻重做。重点重做平面构成、模块形状、空间层级和 3D 感。

目标效果：

- 第一眼不是普通个人网站，而像一个明亮的个人档案主控空间。
- 页面中出现明显的前中后景：背景空间、主体档案、前景控制面板。
- 主要模块不再都是方正矩形，而是包含斜切、错位、悬浮、曲面、透视、局部越界。
- 《触星》项目成为最强的空间化视觉入口。
- 信息密度增加，但每个信息块都有功能，不做无意义堆叠。
- 视觉语言接近《明日方舟》的 UI 设计思路：工业 HUD、空间面板、异形几何、巨型标题、图层遮挡、局部高亮。

## 可直接给 Agent 的提示词

你是一名高级网页视觉设计与前端实现 Agent。请在现有网站基础上继续改造。当前色调已经合格，不要大改配色；请重点解决平面设计过于方正、缺少立体感、缺少《明日方舟》式视觉冲击的问题。

请保留现有内容和栏目，但重新组织首屏、文章区、触星区、追番区、联系区的视觉结构。目标是让页面像一个可操作的个人档案主控台，而不是普通卡片式个人博客。

## 核心设计原则

### 1. 用空间场景替代平面卡片

不要继续把所有内容放在同一平面上的矩形卡片中。请把首屏设计成一个空间场景：

- 背景层：带透视的网格地面、淡色弧形屏幕、远处半透明结构线。
- 中景层：头像档案舱、姓名大标题、个人身份信息。
- 前景层：悬浮导航模块、状态面板、主行动入口、音乐 HUD。

至少要出现以下空间元素中的 3 种：

- 斜向漂浮面板
- 弧形或半圆形屏幕
- 等距视角平台
- 透视网格地面
- 半透明悬浮玻璃片
- 前景遮挡线框
- 漂浮几何块
- 轨道圆环
- 数据光柱或垂直扫描线

### 2. 打破正矩形

目前图形太方正。请减少普通矩形卡片，改用以下形态：

- 切角矩形：`clip-path: polygon(0 0, calc(100% - 14px) 0, 100% 14px, 100% 100%, 14px 100%, 0 calc(100% - 14px))`
- 斜边面板：右侧或底部做 8-16px 的斜切。
- L 形边框：只显示两个角，不画完整方框。
- 弧形信息带：用大圆角外环或 `border-radius: 50%` 的局部裁切做成曲面屏幕。
- 梯形标签：使用 `skewX(-12deg)` 或 `clip-path`。
- 断裂边框：边框不连续，局部断开，像设备面板。
- 越界编号：大号编号溢出卡片边缘，只保留低透明部分。
- 悬浮贴片：小标签压在主面板边缘，不在卡片内部整齐排列。

注意：不是所有组件都异形。正文阅读区可以保持规整，但 Hero、入口模块、触星项目区必须明显打破方正。

### 3. 用遮挡关系制造立体感

请刻意设计前后遮挡：

- 姓名大标题可以被头像圆环、线框、半透明面板局部遮挡。
- 头像档案舱可以压住背景圆环和状态线。
- 入口面板可以部分覆盖主标题区域或背景平台。
- 背景网格线可以从主体后方穿过，但在主体区域被遮挡。
- 触星区的数据模块可以漂浮在主项目面板上方，并投下淡蓝紫阴影。
- 音乐 HUD 可以作为最前景底栏，但不要挡住核心内容。

禁止所有元素互相保持完全独立、没有叠压关系。明日方舟式视觉的关键是“图层互相穿插”。

### 4. 增加大图形，而不是只加小装饰

当前页面小标签和线框不少，但缺少有冲击力的大图形。请增加 2-4 个大尺度图形：

- Hero 背后一个巨大的低透明圆环仪表。
- 右侧一个弧形屏幕或半透明曲面面板。
- 触星区一个等距视角星图平台。
- 文章区背后一个巨型 `CASE FILE` 或罗马数字水印。
- 页面背景中一条斜向高亮轨道线，从左下穿到右上。

这些大图形要低透明，不要抢正文，但必须让页面不再只是小卡片排版。

## 首屏专项改造

当前首屏已有头像圆形档案、姓名、四个 Node 卡片，但仍然平。请改成“档案舱 + 浮动控制台”。

### 布局建议

桌面端首屏可以采用如下结构：

- 左侧：头像档案舱，包含头像、LV、ONLINE、ID、FOCUS。头像外侧使用双层圆环、扫描缺口、状态点。
- 中央：姓名“萧和和”超大标题，标题后方放低透明英文 `PROFILE NODE` 或 `LEVEL DESIGNER`。
- 右侧：一个倾斜的悬浮主面板，显示 `TOUCH THE STARS / MAIN PROJECT` 或当前主项目。
- 下方：4 个入口模块不要等宽排齐，改为大小不同、错位排列的面板组。

### 入口模块形态

不要四个一样大小的方块。建议：

- `触星` 是最大主入口，占 1.4 倍宽，带奶油黄边缘光和 `MAIN` 状态。
- `文章作品` 是左侧中型 CASE 面板，带大号 `01` 水印。
- `追番记录` 是较小媒体档案贴片，带 `SYNCED` 状态。
- `联系方式` 是通信终端小模块，像可点击命令键。

模块位置允许错位：

- 上下错开 12-28px。
- 部分面板可向上覆盖标题下沿。
- 主入口可使用 `transform: perspective(900px) rotateY(-6deg) rotateX(2deg)`。
- 次入口只做轻微倾斜，避免过乱。

### Hero 需要新增的图形层

请实现：

- 一个巨型低透明圆环，位于头像和标题之间。
- 一条斜向高亮线，从头像状态点延伸到触星主入口。
- 背景透视网格，越靠下格子越大，模拟地面。
- 几个漂浮小方块或薄片，带柔和投影。
- 2-3 条垂直扫描线或坐标刻度。

这些元素都要使用现有蓝紫、奶油黄、淡青、浅绿，不要新增大面积颜色。

## 文章区专项改造

当前文章区像白色表格。请改成“档案墙 / Case Board”。

要求：

- 文章卡片不再是普通 2x2 网格。
- 使用一张大底板，底板上放 3 个错位 case 文件。
- Case 01 和 Case 02 可以左右并列，但其中一个向上错位。
- Case 03 横跨底部，但不要只是全宽矩形；可以左侧斜切，右侧露出大号 `III` 水印。
- 每个 case 卡片加入档案夹视觉：顶部标签、切角、装订线、局部半透明贴片。
- 使用 `CASE 01 / LEVEL DESIGN / STATUS PUBLIC` 作为顶部信息带。

图形补充：

- 背后放低透明坐标纸网格。
- 卡片边缘有 L 形角标。
- 阅读链接像系统命令：`VIEW DOCUMENT ->`。
- Hover 时卡片从底板上“抬起”3-6px，并出现淡青边线。

## 触星区专项改造

触星是最需要增强立体感的区域。不要再使用左右白色大面板 + 四格数据表。

请把触星区设计成“主线项目沙盘”：

### 结构

- 背景：星图/轨道/透视网格。
- 中央：一个等距视角平台或漂浮项目核心。
- 左侧：项目介绍文本，像任务 briefing。
- 右侧：4 个数据模块漂浮在不同高度：`3D`、`10+`、`RPG`、`1人`。
- 前景：主 CTA `REQUEST ACCESS / 加入触星计划`，像系统命令按钮。

### 等距平台实现方式

可以用纯 CSS：

```css
.project-platform {
  transform: perspective(900px) rotateX(58deg) rotateZ(-8deg);
  transform-style: preserve-3d;
  clip-path: polygon(8% 0, 100% 0, 92% 100%, 0 100%);
}
```

平台上可以画：

- 星图节点
- 轨道线
- 小型坐标点
- 发光中心
- 连接线
- 漂浮数据标签

### 数据模块

四个数据不要放在整齐表格里。改为：

- `3D` 放在平台右上，像主参数。
- `10+` 放在更远处，透明度略低。
- `RPG` 放在下方前景，稍大。
- `1人` 放在右下小模块，像身份徽章。

每个模块有不同层级和轻微投影，形成远近关系。

## 追番区专项改造

追番区可保持信息规整，但要避免普通封面网格。

建议：

- 外层设计为“MEDIA ARCHIVE 终端”。
- Tabs 做成斜切分段按钮，而不是普通文本按钮。
- 每个番剧卡片保留封面，但添加档案边框、状态点、进度条。
- 卡片可轻微错位，不要所有封面完全同一高度。
- Hover 时显示 `EP / RATE / STATUS` 小面板。

注意追番区以可读性为主，不要做过强 3D，避免封面变形。

## 联系区专项改造

联系区现在过于平。请改成“通信终端”：

- 背景放一个低透明雷达圆环或通信节点图。
- 三个联系方式不是等分矩形，而是一个主邮箱模块 + 两个次级链接模块。
- 邮箱模块最大，带 `PRIMARY CHANNEL`。
- GitHub 和 Bilibili 作为右侧两个叠放的小面板。
- 每个模块有状态：`OPEN`、`PUBLIC`、`EXTERNAL`。
- 增加一个小型命令行输入视觉：`> SEND MESSAGE TO XIAO_HEHE`

## 音乐播放器专项改造

当前底部音乐栏已经接近 HUD，但仍偏普通。请增强立体和图形感：

- 底栏左侧加入斜切 `NOW PLAYING` 标签。
- 播放按钮外侧做圆环仪表，而不是简单圆形。
- 曲名区域像状态屏，带扫描线。
- 音量条做成细长仪表轨道。
- 右侧 `RANDOM` 使用状态灯。
- 底栏可有轻微顶部投影和半透明背景，像浮在页面上方。

移动端请收缩，避免遮挡内容。

## CSS 技术建议

请优先用 CSS 实现空间感，不一定引入 Three.js。

可用技术：

```css
/* 切角 */
clip-path: polygon(0 0, calc(100% - 16px) 0, 100% 16px, 100% 100%, 16px 100%, 0 calc(100% - 16px));

/* 透视悬浮 */
transform: perspective(900px) rotateY(-7deg) rotateX(3deg) translateZ(0);

/* 等距平台 */
transform: perspective(1000px) rotateX(58deg) rotateZ(-8deg);

/* 断裂边框可用伪元素 */
::before, ::after { position: absolute; border: 1px solid var(--panel-line); }

/* 曲面屏幕感 */
border-radius: 50% / 12%;
clip-path: inset(0 0 0 0 round 40% 40% 8% 8%);

/* 扫描线 */
background-image: repeating-linear-gradient(
  90deg,
  rgba(255,255,255,.0) 0,
  rgba(255,255,255,.0) 8px,
  rgba(255,255,255,.18) 9px
);
```

请使用 `box-shadow`、`filter: blur()`、`backdrop-filter` 和透明度建立景深，但不要使用厚重黑影。阴影应是蓝紫或浅灰蓝：

```css
box-shadow:
  0 28px 80px rgba(109, 123, 173, 0.22),
  0 2px 0 rgba(255,255,255,0.55) inset;
```

## 动效补充

请通过动效增强立体感：

- 首屏加载时，背景网格先淡入，头像档案舱从后方推近，面板依次从不同方向滑入。
- 入口面板 Hover 时向前浮起，倾角略微变化。
- 触星平台上的节点有非常轻微的呼吸光。
- 斜向高亮线可以缓慢扫过一次。
- 大号圆环可以极慢旋转，周期 40 秒以上。
- 禁止弹跳、夸张缩放、过度闪烁。

必须支持 `prefers-reduced-motion`。

## 明日方舟感的关键检查

完成后用以下问题验收：

- 页面是否还有大量普通正矩形？如果是，继续减少。
- 首屏是否像一个空间化档案系统，而不是头像介绍页？
- 是否能看到前景、中景、背景的遮挡关系？
- 是否有至少一个强视觉装置：弧形屏幕、等距平台、巨型圆环、漂浮面板组？
- 《触星》是否像主项目入口，而不是文本介绍表格？
- 文章区是否像 Case File 档案墙，而不是普通文章列表？
- 图形元素是否有大小对比，而不是只有小标签？
- 透视和异形是否没有牺牲中文可读性？
- 色调是否保持当前满意方向，没有重新变成暗黑或高饱和霓虹？

## 禁止事项

- 不要继续只加更多细边框和小字标签来假装复杂。
- 不要把所有卡片都做成同样大小的矩形。
- 不要为了 3D 感把正文旋转到难以阅读。
- 不要使用厚重黑影或暗黑赛博背景。
- 不要复制《明日方舟》的角色、Logo、职业图标、原始按钮样式或专有术语。
- 不要让装饰图形遮挡核心文字和按钮。
- 不要把移动端做成缩小版复杂桌面，移动端要简化为清晰档案流。

## 最终一句话目标

把当前网站从“颜色漂亮的平面信息卡片”升级为“具有空间纵深、异形图层、可操作主控台气质的个人档案界面”：色调保持柔和，结构变得大胆，图形不再方正，核心项目《触星》成为一个真正有进入感的视觉场景。
